A real-time messaging application built to explore WebSocket-driven updates, room-based conversations, and persistent chat history in a full-stack environment.

The project demonstrates how to combine live event delivery with durable storage so users can resume conversations without losing context.
