```
React Client
     ↓
Socket.IO Gateway
     ↓
Express API
     ↓
MongoDB
```

The client maintained active socket connections for live updates while the API handled authentication context and message persistence.
