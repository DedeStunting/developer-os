# TODO(Phase 3): Wire ESLint and Prettier checks on pull_request

name: Lint (placeholder)

on:
  workflow_dispatch:

jobs:
  placeholder:
    runs-on: ubuntu-latest
    steps:
      - run: echo "TODO(Phase 3): Implement lint workflow — see comments in this file"
